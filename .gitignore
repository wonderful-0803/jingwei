# The dev branch tracks development assets, including docs, tests and lockfiles.
# Cargo package exclusions are maintained separately in Cargo.toml.
# Never commit target build directories, at any depth.
target/
