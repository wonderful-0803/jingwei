# JW-08：内部评测与真实模型基线

日期：2026-09-14。接续 JW-07-d；用户确认暂停 JW-07-e，优先实施 JW-08-a。有限参考 Agent 作为现有执行组件复用，本阶段不扩展其功能。

| 批次 | 范围与状态 |
| --- | --- |
| JW-08-a（已实施） | 内部任务运行器、四个跨文档/订单场景试跑任务、fake/现有模型服务接入、逐条记录与汇总；见 [实施记录](JW-08-a.md) |
| JW-08-b（待实施） | 固定至少 40 个任务；确认设备、模型及服务端参数，完成两模型各配置三轮的首份真实基线，保留失败 |
| JW-08-c（待实施） | 上下文/约束/纠错消融、失败分析、阈值冻结及内部资产交付边界核对 |

每批只做相关测试和必要静态检查；真实模型执行先试跑，再集中评测，不在普通开发修改后重复整个矩阵。JW-07-e 的恢复故障验收仍待完成，不能跳过后宣称完整 AT-F5/F6 通过。公开交付边界、剩余内嵌测试迁移及内部备份证明也须在正式验收补齐。

JW-08-a 不启动模型、不下载权重、不选择付费服务；真实运行前由宿主确认 endpoint、已安装模型、设备和运行成本。当前适配器不设置采样 temperature/seed，正式基线必须核实/固定服务端参数或另行补齐接口。
