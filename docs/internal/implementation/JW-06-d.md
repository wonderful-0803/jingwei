# JW-06-d：有限参考 Agent 整体验收

日期：2026-09-14。起点 dev / 94c0512，账户 wonderful-0803。验收映射见 [JW-06 / F3](JW-06-acceptance.md)。

## 交付

本批补齐测试与文档，不改变生产 API、默认策略或依赖。新增内部完整装配示例：第一个工具生成不可预置的票据，模型从实际结果视图读取票据并调用第二个工具；同一 Harness 可换为完全自定义的确定性 Agent，继续使用相同 runtime 和工具授权。

新增 7 项测试入口：单工具且持久化结果可查询；真实数据依赖及自定义 Agent 对照；真实用户审批拒绝且不得换另一个已授权工具；过期/时钟/结果视图/无效动作停止；runtime 关闭排空；工具结果持久化及核对失败保留类型化证据；旧历史缺失完整回复时停止。Native/Json 均覆盖五条公共路径。

为既有参考 Agent、纠错测试增加统一 canonical 收尾断言，检查事件配对、顺序、唯一终态、预算确认计数与无悬挂资源。持久化不可闭合路径单独检查失败尝试和真实工具证据，不套用正常闭合结论。

## 验证

[Linux GNU 完整基线](../../../results/baseline/20260914-152937-934310-3mp7hyas/summary.json)：首次完整运行 9/9 通过，377 项 Rust 测试（14 root、342 private、21 guide doctest）。参考 Agent 集成测试共 33 项入口，包含本批新增 7 项。

VitePress 根路径及 /jingwei/ 构建、Markdown 测试、链接/搜索产物检查通过：每份 20 页、841 链接、3 搜索查询、0 公开源码文件。默认 facade 依赖树仍不包含 reference-agent/action/context；生产包和锁文件无改动。

开发中将持久化故障注入完善为“写入不确定且核对读取也失败”：既有 Session runtime 能通过物理核对证明未提交，不能将可核对的错误误报为日志不可闭合。旧日志用例在 Session 加载前预置历史，避免只修改持久化测试存储却仍读取 runtime 缓存。上述调整均为测试准确性修正，生产行为未改变。

## 接续

JW-06 a–d 在本地契约范围内完成，下一阶段进入 JW-07：完整 Task 状态与恢复。继续保留宿主业务验证、待答所有权、持久化执行 lease 与真实模型/平台评测边界。
