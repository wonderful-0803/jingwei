# JW-06-b：完成检查与无进展检测

日期：2026-09-14。起点 dev / f4494b6，账户 wonderful-0803。决策见 [ADR-0017](../adr/0017-reference-completion-progress.md)。

## 交付

ReferenceAgent 增加可选 with_checks 装配；CompletionChecker 区分未验证声明、带宿主证据的完成和拒绝。拒绝立即结束，非法或过大证据停止，不额外推理。ReferenceState 提供宿主业务版本，默认未知，替代此前每步变化的合成版本。

ProgressPolicy 默认第三次连续相同动作/参数/完整反馈/业务版本后停止，支持按工具设置有限轮询阈值。每 Turn 保留一份有界观察；超限停止并保留已确认工具和步骤记录。调用与事件 ID 不参与语义比较。业务状态、参数变化重置计数；总步数、Task 预算与原运行时收尾不变。

run_v1 新增 completion/repeated_observations，新读取器兼容旧报告；新增停止原因需要旧严格读取器升级。公开指南新增可编译的回执检查器示例。

## 验证

[Linux GNU 完整基线](../../../results/baseline/20260914-144434-714666-q7pfewl7/summary.json)：9/9 通过，362 项 Rust 测试（14 root、328 private、20 guide doctest）。新增 6 项测试入口覆盖 Native/Json 的完成证据、拒绝与非法证据、连续第三次结果停止、有限轮询和状态变化、观察超限后证据保留、旧报告读取、参数变化打断重复；既有取消、预算、工具拒绝与报告写入失败测试继续通过。

开发时修正了新增测试把预期失败误用 unwrap 的断言，运行时失败契约未改变。完整基线首次即通过。默认 facade 依赖树仍不包含参考 Agent，无新增第三方依赖。

VitePress 根路径及 /jingwei/ 构建、Markdown 测试和产物检查通过：每份 20 页、839 链接、3 搜索查询、0 公开源码文件。

## 接续

下一批 JW-06-c：有限结构化修正与共享修正预算，以及更完整的待答信息和宿主续跑契约。本批不检测交替循环、不持久化检测状态，也不推断业务证据真实性；任意同步宿主代码的强制终止不在有限步数保证范围内。
