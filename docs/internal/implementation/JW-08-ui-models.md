# Harness 工作台：本地 GGUF 切换

2026-09-14，基于 cb52a7f。模型目录固定默认为用户指定的 luxiclaw-runtime/models，可通过宿主参数修改。新增目录扫描、软链接去重/范围校验、文件大小展示、刷新、异步 CUDA 进程替换、健康检查、失败状态与日志、记忆最后加载模型。新模型名称由服务实际加载结果决定，不接受浏览器用其他别名冒充；任务期间与桥接请求未结束时禁止切换。

工作台 server 统一持有模型进程，start.py 转为启动参数入口，关闭时只释放自己启动的服务。历史展示 model，逐任务原配置与服务 props 保留模型身份。未选择已就绪模型时禁止真实测试。仅扫描根目录 GGUF；不声称所有 GGUF 都支持当前对话协议。

验证：2 项模型目录/切换互斥测试和 3 项既有工作台集成测试通过。浏览器实际完成 Qwen3-8B → Qwen3.8-4B 的 CUDA 切换，并运行一条新任务确认请求 model 与所加载文件一致；语法和 whitespace 检查通过。未跑完整框架回归。
